date : 15.09.2025
    was ist Single page App
    was ist AAA
    was ist Webserver
    wiederholung HTTP Request life cylce

step 1: Clean up : löschen all unnötigten datai für unsere Projekt . nur App.js , index.css , index.js and reportWebVitals.js 
bleibt dort.
also wir haben alle linen in index.css entfernt

2: installation von tailwind css
   2.1 : npm install -D tailwindcss@3
   2.2 : npx tailwindcss init   
   2.3 : in tailwind.config.js 
                /** @type {import('tailwindcss').Config} */
                module.exports = {
                content: [
                    "./src/**/*.{js,jsx,ts,tsx}",
                ],
                theme: {
                    extend: {},
                },
                plugins: [],
                }
    2.5: in index.css 
            @tailwind base;
            @tailwind components;
            @tailwind utilities;

3: more clean up . remove performance motitoring , also create app folder structure . 
then install react router dom: 
npm install react-router-dom

why npm install react-router-dom ??  because if we are developing web application , we shall install router-dom , dom is for :
Document Object Model , it is for Web , install react-router do not work on web! because it is for 
cross platform applications. 

Aufgabe: 
verlinken Alle Siten! dann mit url , zugriffbar sein!

erstelle ine default route mit einer 404 seite





